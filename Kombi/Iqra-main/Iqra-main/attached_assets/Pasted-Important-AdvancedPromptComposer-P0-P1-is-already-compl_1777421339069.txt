Important:
AdvancedPromptComposer P0/P1 is already completed and smoke-tested.

Do not refactor the existing composer core.
Do not change verified behavior:
- quickPrompt persistence
- send gating
- IME guard
- Cmd/Ctrl+K
- activePopover single-popover rule
- normalized payload
- lastSetBy guards

This task only adds missing management/source-selection layers:
- Directory modal
- ProjectPicker
- Screenshot action
- GitHubSourcePicker
- Company Knowledge shortcut
- StylePicker
- ModelSelector
- ToolAccessMenu
- Plugin browsing

Regression guard:
After implementation, rerun the existing AdvancedPromptComposer smoke tests.
The task is not complete if any previously passing test fails.

========================================
ADVANCED COMPOSER — MISSING MANAGEMENT LAYERS PATCH
========================================

Continue refining the existing AdvancedPromptComposer.

Do not redesign the composer shell.
Do not duplicate existing systems:
- Do not create a second attachment state.
- Do not create selectedConnectors.
- Do not create selectedGitHubSources.
- Do not create selectedCompanyKnowledge.
- Do not duplicate SkillPicker, ContextPicker, ToolPolicyMenu, ThemePicker or AttachmentMenu.

This patch adds only the missing non-redundant capability layers:
- Directory modal
- Project association
- Screenshot action
- Direct GitHub source picker
- Company Knowledge shortcut
- Style selector
- Model selector
- Tool access policy
- Plugin browsing
- Management actions

========================================
1. QUICK ACTION MENU EXTENSION
========================================

Update Quick Action Menu so it supports these rows:

Primary rows:
1. Team Mode
2. Attachments
3. Connectors
4. Company Knowledge
5. Deep Research
6. Web Search
7. Take a screenshot
8. Add to project
9. Add from GitHub
10. Use style
11. Race Mode
12. Theme
13. More / Directory

If this becomes visually too tall:
- Keep Team Mode, Attachments, Connectors, Company Knowledge, Deep Research, Web Search, Add to project and More visible.
- Move Screenshot, GitHub, Style, Race Mode and Theme into More.

Rules:
- Only one popover may be open at a time.
- All actions must reuse existing state.
- Directory modal closes activePopover when opened.

========================================
2. COMPANY KNOWLEDGE / VIRKSOMHEDSVIDEN
========================================

Add Company Knowledge as a first-class shortcut into ContextPicker.

Label:
- English: Company Knowledge
- Danish allowed: Virksomhedsviden

Behavior:
- Opens ContextPicker with company knowledge filter active.
- Adds selected items to selectedContextItems.
- Shows chip:
  "Company Knowledge"
  or
  "Context: Company Knowledge"

Extend ContextItem sourceType:

sourceType:
  | "recent-chat"
  | "artifact"
  | "file"
  | "memory"
  | "company-knowledge"
  | "connector"
  | "web"

Do not create selectedCompanyKnowledge.

Mock company knowledge:
- Brand guidelines
- Biyocon operating principles
- Tender response standards
- OmniAI product architecture
- User/team preferences

========================================
3. PROJECT PICKER
========================================

Add ProjectPicker.

Open from:
- Quick Action Menu → Add to project

activePopover = "project"

ProjectPicker UI:
- Floating submenu on desktop
- Bottom sheet on mobile
- Searchable list
- Scrollable
- Project/folder icon
- Project name
- Workspace/org label underneath

Mock projects:
- kvalifikationsordning Fri
- Visual FoxPro
- Brand Biyocon
- Test
- To in-house konsulenter til vejafvan…
- Vejdirektoratet - Tilbud - Fagtilsyn - …
- Lokaltog_Kvalifikationsordning_Tek…

State:

selectedProject?: {
  id: string
  name: string
  workspace?: string
}

Behavior:
- Selecting a project adds chip: "Project: {name}"
- Removing chip clears selectedProject.
- Project does not modify prompt, mode, model, outputTarget or conversationType.
- Project is not automatically added as context.

Payload:

project?: {
  id: string
  name: string
  workspace?: string
}

========================================
4. SCREENSHOT CAPTURE ACTION
========================================

Add "Take a screenshot" action.

This is mock-only.

Behavior:
- On click, create a mock complete image attachment using existing attachments state.
- Do not create screenshotAttachments.

Mock attachment:

{
  id,
  name: "screenshot.png",
  type: "image",
  mimeType: "image/png",
  extension: "png",
  size,
  source: "local",
  status: "complete",
  progress: 100
}

The screenshot must appear as a normal attachment chip and be sent through normal attachment payload normalization.

========================================
5. DIRECT GITHUB SOURCE PICKER
========================================

Add "Add from GitHub" action.

activePopover = "github-source"

GitHubSourcePicker UI:
- Search input
- Recent repositories
- Mock branch selector
- Mock file/path list
- Add selected files/repos as context items

Mock repos:
- Biyocon/Ai-Agent-RAG
- Biyocon/Ai-agent-platform
- Biyocon/SkillMatch
- Biyocon/ai-dev-tasks

When selected, add to selectedContextItems:

{
  id,
  title,
  sourceType: "connector",
  connectorId: "github",
  sourceName: "GitHub",
  status: "available",
  selected: true,
  lastUpdated
}

Do not create selectedGitHubSources.
GitHub selected sources must appear in payload under contextItems.

========================================
6. STYLE SELECTOR
========================================

Add "Use style" submenu.

Style is not Theme.

Theme = visual/output styling.
Style = response tone and communication format.

activePopover = "style"

Style options:
- Normal
- Learning
- Concise
- Explanatory
- Formal

State:

selectedStyle?: {
  id: "normal" | "learning" | "concise" | "explanatory" | "formal"
  label: string
}

Behavior:
- Selecting style adds chip: "Style: {label}"
- Removing chip resets style to undefined or Normal.
- Style must not overwrite prompt text.
- Style must not overwrite Theme.
- Style must not change selectedMode.

Payload:

style?: {
  id: string
  label: string
}

========================================
7. MODEL SELECTOR
========================================

Add ModelSelector as a separate execution control.

Do not merge Model with Mode.

Mode remains:
- Instant
- Fast
- Expert
- Heavy
- Auto

Model means concrete model choice.

ModelSelector UI:
- Trigger shows selected model, e.g. "Sonnet 4.6"
- Dropdown items:
  - Opus 4.7
    Description: Most capable for ambitious work
  - Sonnet 4.6
    Description: Most efficient for everyday tasks
  - Haiku 4.5
    Description: Fastest for quick answers
- Selected checkmark
- Divider
- Adaptive thinking toggle:
  Label: "Adaptive thinking"
  Description: "Thinks for more complex tasks"
- More models submenu:
  - Opus 4.6
  - Opus 3
  - Sonnet 4.5

State:

selectedModel?: {
  id: string
  label: string
  description?: string
}

adaptiveThinkingEnabled: boolean

Behavior:
- Model selection updates selectedModel.
- Adaptive thinking updates adaptiveThinkingEnabled.
- Model does not overwrite selectedMode.
- selectedMode does not overwrite selectedModel.

Payload:

model?: {
  id: string
  label: string
}

adaptiveThinkingEnabled: boolean

========================================
8. TOOL ACCESS POLICY
========================================

Extend existing toolsPolicy with toolAccessMode.

Do not replace toolsMode.

State:

toolAccessMode:
  | "load-when-needed"
  | "preload-tools"

Default:
- "load-when-needed"

ToolAccessMenu:
- Load tools when needed
  Description: "Tools are loaded only when relevant."
- Tools already loaded
  Description: "Enabled tools are preloaded for this chat."

Payload:

toolsPolicy: {
  toolsMode,
  allowedTools,
  requireConfirmation,
  toolAccessMode
}

Open from:
- ToolPolicyMenu footer
- Connector submenu footer
- Quick Action Menu → More if needed

========================================
9. DIRECTORY MODAL
========================================

Add Directory modal for browsing and managing:

- Skills
- Connectors
- Plugins

This is a management surface.
It does not replace small pickers.

Open Directory from:
- SkillPicker footer → Manage skills
- Connector picker/footer → Manage connectors
- Plugin row/footer → Manage plugins
- Quick Action Menu → More / Directory
- Slash commands:
  /directory
  /skills
  /connectors
  /plugins

When Directory opens:
- close activePopover
- directoryState.isOpen = true

Directory layout:
- Large centered modal
- White background
- Dark overlay
- Rounded corners
- Close X top-right
- Left sidebar:
  - Skills
  - Connectors
  - Plugins
- Main area:
  - Search input
  - Category pill
  - Filter by dropdown
  - Sort by dropdown
  - 2-column card grid on desktop
  - 1-column on mobile

State:

directoryState: {
  isOpen: boolean
  activeTab: "skills" | "connectors" | "plugins"
  searchQuery: string
  selectedCategory: string
  filterBy?: string
  sortBy?: string
}

========================================
10. DIRECTORY TABS
========================================

Skills tab cards:
- name
- provider
- usage/download count
- short description
- settings/gear icon
- install/add button where relevant

Mock skills:
- /canvas-design
- /web-artifacts-builder
- /mcp-builder
- /theme-factory
- /doc-coauthoring
- /brand-guidelines
- /internal-comms
- /algorithmic-art
- /slack-gif-creator
- /skill-creator

Connectors tab cards:
- connector name
- icon
- popularity/status text
- short description
- plus button or settings button
- connected state

Mock connectors:
- Google Drive
- Gmail
- Google Calendar
- Figma
- Microsoft 365
- Box
- HubSpot
- Intercom
- Spotify
- monday.com
- Atlassian Rovo
- Autodesk Product Help

Plugins tab:
Show informational banner:
"Plugins can be browsed, but are only available for use in the desktop app."

Plugin cards:
- plugin name
- provider
- usage/download count
- short description
- add/install or disabled state

Mock plugins:
- Design
- Productivity
- Marketing
- Data
- Engineering
- Finance
- Product management
- Operations
- Legal
- Sales

========================================
11. PLUGIN SUPPORT
========================================

Plugins are distinct from:
- Skills
- Connectors
- Tools

Plugin model:

{
  id: string
  name: string
  description: string
  provider?: string
  enabled: boolean
  availability: "web" | "desktop-only" | "mock"
}

Behavior:
- Plugins appear in Directory modal.
- Desktop-only plugins can be browsed but not activated in web.
- Enabled web/mock plugins show chip:
  "Plugin: {name}"

Payload:
Only enabled web/mock plugins are included.

plugins?: {
  id: string
  name: string
}[]

Do not merge plugins into skills.
Do not merge plugins into connectors.

========================================
12. MANAGEMENT ACTIONS
========================================

Add footer/management actions.

SkillPicker footer:
- Manage skills
- Add skill

Connector picker/footer:
- Manage connectors
- Add connector
- Tool access

Directory:
- Manage skills opens Directory on Skills tab.
- Manage connectors opens Directory on Connectors tab.
- Manage plugins opens Directory on Plugins tab.

Add actions:
- Mock-only
- Show toast or placeholder modal
- No backend creation

========================================
13. ACTIVE POPOVER EXTENSION
========================================

Extend activePopover with:

| "project"
| "github-source"
| "style"
| "model"
| "tool-access"

Existing values remain:

null
| "slash"
| "attachment"
| "context"
| "skill"
| "tool"
| "template"
| "output"
| "mode"
| "conversation"
| "quick-actions"
| "theme"
| "race"

Rules:
- Only one popover may be open at a time.
- Directory modal uses separate directoryState.
- Opening Directory closes activePopover.

========================================
14. NORMALIZED PAYLOAD EXTENSION
========================================

Extend normalized submit payload with:

{
  project?: {
    id: string
    name: string
    workspace?: string
  }

  style?: {
    id: string
    label: string
  }

  model?: {
    id: string
    label: string
  }

  adaptiveThinkingEnabled: boolean

  plugins?: {
    id: string
    name: string
  }[]

  toolsPolicy: {
    toolsMode,
    allowedTools,
    requireConfirmation,
    toolAccessMode
  }
}

Rules:
- Do not send full project objects.
- Do not send full style definitions.
- Do not send full plugin definitions.
- Do not send full model metadata.
- Do not send full connector registry.
- Company Knowledge and GitHub selections must appear in contextItems.
- Screenshot must appear in attachments.

========================================
15. FILE STRUCTURE
========================================

Add:

/components/composer/popovers/ProjectPicker.tsx
/components/composer/popovers/GitHubSourcePicker.tsx
/components/composer/popovers/StylePicker.tsx
/components/composer/popovers/ModelSelector.tsx
/components/composer/popovers/ToolAccessMenu.tsx

/components/composer/directory/DirectoryModal.tsx
/components/composer/directory/DirectorySidebar.tsx
/components/composer/directory/DirectoryCard.tsx
/components/composer/directory/SkillsDirectoryTab.tsx
/components/composer/directory/ConnectorsDirectoryTab.tsx
/components/composer/directory/PluginsDirectoryTab.tsx

/hooks/useDirectory.ts
/hooks/useProjectPicker.ts
/hooks/useStylePicker.ts
/hooks/useModelSelector.ts

Extend:
/types/composer.ts
/data/mockComposerData.ts

========================================
16. MOCK DATA
========================================

Add centralized mock data:

mockProjects
mockCompanyKnowledgeItems
mockStyles
mockModels
mockMoreModels
mockToolAccessModes
mockPlugins
mockDirectorySkills
mockDirectoryConnectors
mockDirectoryPlugins
mockGitHubSources

Do not hardcode arrays inside UI components.

========================================
17. FINAL ACCEPTANCE CRITERIA
========================================

Implementation is complete when:

- Company Knowledge opens ContextPicker filtered to company knowledge items.
- Company Knowledge selections flow through selectedContextItems.
- ProjectPicker exists and selected project appears as chip.
- Selected project is included as lightweight payload metadata.
- Take screenshot creates a mock complete image attachment through existing attachment state.
- Add from GitHub opens GitHubSourcePicker.
- GitHub selected items flow through selectedContextItems.
- StylePicker exists and selected style appears as chip.
- Theme and Style remain separate concepts.
- ModelSelector exists and is separate from Mode dropdown.
- Adaptive thinking toggle exists and is included in payload.
- ToolAccessMenu exists and updates toolsPolicy.toolAccessMode.
- Directory modal exists with Skills, Connectors and Plugins tabs.
- Manage skills opens Directory on Skills tab.
- Manage connectors opens Directory on Connectors tab.
- Plugins are browseable but distinct from skills/connectors.
- Desktop-only plugins are visibly disabled or mock-only.
- No duplicate attachment state exists.
- No selectedConnectors state exists.
- No selectedGitHubSources state exists.
- No selectedCompanyKnowledge state exists.
- Payload sends only lightweight metadata for project, style, model, plugins, theme, template and quickPrompt.
- Company Knowledge and GitHub context are sent through contextItems.
- Screenshot is sent through attachments.
- All new popovers follow activePopover single-popover rule.
- Directory modal closes activePopover when opened.
- Existing AdvancedPromptComposer smoke tests still pass.